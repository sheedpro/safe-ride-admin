import { Button, Divider } from '@fluentui/react-components';
import { CheckmarkCircleRegular, SignOutRegular } from '@fluentui/react-icons';
import { navigation, can } from '../config/navigation';
export function AppShell({ admin, page, setPage, onLogout, children }) { const initials = admin.name.split(' ').map(name => name[0]).join('').slice(0, 2); return <div className="shell"><aside><div className="brand"><span>SR</span><div>SAFERIDE<small>OPERATIONS CONSOLE</small></div></div><nav>{navigation.filter(([, , permission]) => can(admin.role, permission)).map(([label, Icon]) => <button key={label} className={page === label ? 'active' : ''} onClick={() => setPage(label)}><Icon/><span>{label}</span></button>)}</nav><div className="sidebar-foot"><Divider/><button className="account" onClick={onLogout}><span>{initials}</span><div>{admin.name}<small>{admin.role.replace('_', ' ')}</small></div><SignOutRegular/></button></div></aside><main><header><div className="crumb"><span>SAFERIDE</span><b>/</b>{page}</div><div className="system-status"><CheckmarkCircleRegular/> Protected admin session</div></header><div className="content">{children}</div></main></div>; }
