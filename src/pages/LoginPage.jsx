import { useState } from 'react';
import { Button, Input, Label } from '@fluentui/react-components';
import { EyeRegular, EyeOffRegular } from '@fluentui/react-icons';
import { configured, supabase } from '../api';
export function LoginPage({ onSession }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [visible, setVisible] = useState(false); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function submit(event) { event.preventDefault(); setLoading(true); setError(''); const { data, error: authError } = await supabase.auth.signInWithPassword({ email, password }); setLoading(false); if (authError) setError(authError.message); else onSession(data.session); } return <main className="login"><section className="login-intro"><div className="login-mark">SR</div><p className="eyebrow">SAFE RIDE</p><h1>Operations console</h1><p>Secure access for authorised Veritas and UPF operations staff.</p></section><form onSubmit={submit}><div className="login-form-head"><p className="eyebrow">SIGN IN</p><h2>Welcome back</h2><p>Use your approved administrator account.</p></div>{!configured && <div className="form-error">Set the required dashboard environment variables before signing in.</div>}<Label>Email address<Input value={email} onChange={(_, data) => setEmail(data.value)} type="email" autoComplete="email" required /></Label><Label>Password<Input value={password} onChange={(_, data) => setPassword(data.value)} type={visible ? 'text' : 'password'} autoComplete="current-password" contentAfter={<Button appearance="transparent" size="small" icon={visible ? <EyeOffRegular/> : <EyeRegular/>} aria-label={visible ? 'Hide password' : 'Show password'} onClick={() => setVisible(value => !value)} />} required /></Label>{error && <div className="form-error">{error}</div>}<Button appearance="primary" type="submit" disabled={!configured || loading}>{loading ? 'Signing in…' : 'Sign in'}</Button></form></main>; }
